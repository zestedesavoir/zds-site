import "@fortawesome/fontawesome-free/css/all.min.css";
import.meta.glob('@lib/@fortawesome/fontawesome-free/webfonts/*', {
  eager: true, // This ensures that all images are imported eagerly (without waiting)
});
